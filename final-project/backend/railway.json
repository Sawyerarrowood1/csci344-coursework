{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "DATABASE_PROVIDER=postgres npm run generate -- --yes && DATABASE_PROVIDER=postgres npm run seed && DATABASE_PROVIDER=postgres node src/server.js"
  }
}
